# Noemion 首页设计规范

状态：已实施，桌面端与移动端视觉验收通过
范围：`index.html` 项目门户及其必要的共享外壳、样式和交互

## 1. 设计目标

首页要在保留 Noemion 工程严谨性和成熟度边界的前提下，以清晰的空间节奏、内容层级和交互手感，让第一次访问的读者依次理解：现实缺口是什么、Noemion 的直白结论是什么、对象工具链如何回应、当前做到哪一步、下一步应阅读什么。

首页采用以下设计关系：

- 约 1180–1200px 的居中长页面画布，桌面端保留稳定外侧留白。
- 顶部横向导航、左右分栏首屏、双栏论点区、居中章节标题、横向入口行和三列网格的交替节奏。
- 大字号但克制的标题、细分隔线、近白纸面、少量高饱和强调色和低圆角几何表面。
- 导航、入口行、箭头、图片和按钮在 150–300ms 内完成的快速反馈。
- 移动端按同一阅读顺序折叠为单列，而不是缩小桌面布局。

所有抽象对象图形、流程图、内容和色彩系统都必须服务于 Noemion 自身语义。

## 2. 首页在全站系统中的角色

首页承担全站唯一的完整“叙事型门户”，但不再是唯一采用该设计语言的页面。全站共享顶部导航、连续画布、编辑式排版和交互语言；列表、文章、工具、指南和手册页面分别采用适合其职责的页面范式，详细映射统一维护在 README。首页不承担规范定义，也不把候选方案、未来能力或研究愿景写成已完成成果。

首页首屏必须先用普通语言回答现实问题，再出现 NIR、NOBJ、HOBJ、Deterministic 和 实现化 等术语。所有能力描述继续标注成熟度；当前仍明确处于“设计与安全核心阶段”，没有可安装编译器、Fulfillment Runtime 或正式规范版本。

## 3. 页面结构映射

| 结构角色 | Noemion 首页映射 | 主要内容与目标 |
| --- | --- | --- |
| 顶部品牌与主导航 | Noemion 顶部导航 | 品牌；项目、架构设计、规范、工具、指南入口；移动端折叠菜单 |
| 左右分栏首屏 | “从人类意图到机器表征对象”首屏 | 左侧主张和状态，右侧为 NIR/NOBJ 对象生命周期抽象图形；不使用人物图 |
| 双栏核心论点 | “意义还没有成为工程对象” | 左侧强结论，右侧解释目标、约束、依赖与复现四个现实缺口 |
| 居中栏目标题 | “把一次性上下文变成可验证对象” | 用一句话承接问题与工具链方案 |
| 四条精选内容行 | 四条核心阅读路径 | 背景与边界、对象生命周期、NIR/NOBJ、Deterministic Noesis Core；每行包含图形、标题、摘要和圆形箭头 |
| 三列当前内容网格 | “当前设计重点” | NIR 语义、NOBJ/HOBJ 对象工程、安全二进制核心；状态必须写成待验证方案、开放问题或阶段目标 |
| 第二个大标题与课程行 | “从理解行为到可信装载” | 人类需求、确定性编译、对象链接、受约束运行的连续阅读路径 |
| 开始阅读行动区 | “先理解边界，再阅读对象” | 主按钮进入入门指南，次入口查看规范成熟度；不展示当前不存在的安装或订阅能力 |
| 原则引述区 | “证据优先于主张” | 用简短原则说明规范、ADR、可复现测试和安全验证是判断依据，不使用背书式主张 |
| 个人介绍区 | “Noemion 是什么，也不是什么” | 名称来源、工程映射、非目标和边界；哲学启发与正式技术定义明确区分 |
| 验证路径网格 | “从规范条款到可复现结果” | 规范、ADR、一致性测试、Fuzz、checked arithmetic、可复现性；表达要求，不暗示已经全部达成 |
| 底部行动区与页脚 | 项目入口与站点页脚 | 文档、工具、开发、FAQ 等稳定入口和当前阶段说明 |

### 3.1 分屏文案与链接蓝图

以下文案是实施时的首页信息基线。允许为排版做短句级压缩，但不得改变成熟度、权威性或信任边界。

| 屏段 | 标题或主张 | 支撑内容 | 主入口 |
| --- | --- | --- | --- |
| HERO 首屏 | **从人类意图，到机器表征对象。** | 把一次性的提示词上下文，编译为可分析、验证、重定位、链接和安全装载的目标与约束。状态标签固定为“设计与安全核心阶段”。 | `docs/getting-started.html`、`architecture/noema-lifecycle.html` |
| 01 核心缺口 | **意义还没有成为工程对象** | 提示词擅长表达，但目标、硬约束、证据、权限和验收条件仍埋在文本中；右栏解释目标与措辞、约束、依赖和复现问题。 | `about/background.html` |
| 02 对象路径 | **把一次性上下文变成可验证对象** | 四条横向入口分别解释背景与边界、对象生命周期、NIR/NOBJ 和 Deterministic Noesis Core；摘要只回答一个问题。 | `about/background.html`、`architecture/noema-lifecycle.html`、`specifications/`、`components/noesis-core.html` |
| 03 当前重点 | **当前设计重点** | 三列标记待验证的 NIR、Noema/Horizon Object 以及第一阶段安全二进制核心，不使用发布态措辞。 | `specifications/noema-ir.html`、`specifications/index.html`、`development/implementation-roadmap.html` |
| 04 生命周期 | **从理解行为到可信装载** | Source → Noesis Core → NIR/NOBJ → Object System → Agent Harness → Fulfillment Runtime；Runtime 只评价候选。 | `architecture/noema-lifecycle.html` |
| 05 开始阅读 | **先理解边界再阅读对象** | 主行动进入入门指南；次行动进入规范成熟度登记。当前没有下载按钮、安装命令或版本承诺。 | `docs/getting-started.html`、`specifications/index.html` |
| 06 证据原则 | **证据优先于主张** | 规范条款必须可测试，格式和语义决定必须记录 ADR，安全主张必须对应拒绝路径、Malformed/Fuzz 与一致性证据。 | `development/testing.html` |
| 07 名称与思想 | **Noemion 是什么也不是什么** | Noemion 从 Noema 衍生；Noesis/Noema 只提供理解与对象的工程类比。思想资源产生问题和反例，不能直接产生规范条款。 | `about/intellectual-foundations.html` |
| 08 验证路径 | **从规范条款到可复现结果** | 六格分别为规范、ADR、一致性、Fuzz、checked arithmetic 和可复现性，只表达所需材料与验证方向。 | `development/index.html`、`development/testing.html` |
| 09 页尾 | **选择下一步** | 架构、规范、工具、文档、开发和 FAQ 六组稳定入口；页脚只保留发现入口与主题选择。 | 对应一级入口 |

### 3.2 首页导航信息架构

桌面顶部导航保持一行，按读者任务而不是仓库目录机械排列：

1. 品牌 `Noemion` 返回首页。
2. `项目` 展开项目背景、架构设计、思想与方法以及常见问题。
3. `规范` 展开对象规范、架构决策与开放问题。
4. `工具` 展开工具目录与关键生命周期工具。
5. `指南` 展开入门指南、架构设计指南、开发指南、工具参考指南和规范参考指南。
6. `开发` 展开当前阶段、开发路线图、测试与验证、资源和项目动态。
7. 右侧状态入口只显示当前阶段的 3–4 字短值，以卡片内部动画表达正在进行，并指向 `development/current-stage.html`。

839px 及以下只保留品牌、状态短标和菜单按钮；非门户页的菜单按钮与面包屑保持同一行。菜单展开后显示同一组入口，不复制另一套链接数据；页面背景冻结，导航内部可以独立滚动，点击外部、页面外滑动或 Escape 均可带收回动效关闭。

### 3.3 图形资产语义

| 图形 | 构图 | 必须表达 | 禁止表达 |
| --- | --- | --- | --- |
| 首屏主图 | 右侧纵向“语义频谱”中悬浮一组可链接对象切片；左侧 `SOURCE / BIND / RELOC / VERIFY` 数据轨道将不同状态的数据包推向确定性核心，随后收敛到带符号和重定位标记的对象 | 表达过程与对象分离、底层数据状态逐步检查、歧义被保留、确定性核心决定布局 | 人脑、机器人、魔法粒子、虚构产品界面、人物肖像或伪装成真实遥测的数据面板 |
| 背景与边界缩略图 | 多段文字指向同一目标身份，但硬约束独立成层 | 措辞不等于语义身份 | 把 Prompt 描述成无价值或应被取代 |
| 生命周期缩略图 | Source、Noesis Core、Object、Object System、Agent Harness、Fulfillment Runtime 六段路径 | 确定性边界、能力边界和不可信输入边界 | 模型直接连接 NOBJ 输出或未经策略调用能力 |
| NIR/NOBJ 缩略图 | 左侧目标/约束图，右侧 Section/符号/重定位对象表 | 语义层与对象格式分工 | 冻结尚未决定的字段编号或 ABI |
| Deterministic 缩略图 | 相同输入两次生成相同摘要，模型路径被隔离在候选区 | 无模型、确定、可复现 | 宣称已有性能、兼容性或安全认证结果 |

图形优先使用自有 SVG，并沿用现有 `glass-mint-underlay.svg`、如意云纹和 favicon 的几何语言；可以重新组合视觉语汇，但不直接把现有装饰资产当作技术证明。

“背景与边界缩略图”，也就是“表达方式，不等于语义身份”卡片左侧图形，内部可见标签统一使用简洁英文，例如 `CREATE REPORT`、`WRITE ANALYSIS` 和 `SAME GOAL IDENTITY`。该约束不适用于首屏主图；首屏仍可通过中英文组合表达输入语义，同时保留中文无障碍说明。

## 4. 视觉系统

### 4.1 画布与网格

- 桌面主画布上限 1200px，整体居中，外侧背景与纸面通过极轻色差区分。
- 门户内容区不再套现有 1000px 白色正文卡片；分区直接构成连续画布，并以 1px 低对比分隔线建立节奏。
- 首屏与关键介绍区使用 1:1 或接近 5:4 的双栏；内容行采用“图形 24% / 文本弹性区 / 44px 行动按钮”。
- 三列网格在宽桌面等分；中等宽度改为两列；移动端全部单列。
- 桌面分区纵向留白以 80–112px 为主，内容行以 28–40px 内边距为主；移动端分区留白收敛到 48–80px。

### 4.2 字体与层级

- 延续系统无衬线字体，标题使用 600–700 字重，正文使用 400–500 字重，避免依赖外部商业字体。
- 桌面主标题约 56–64px，移动端约 44–48px；章节标题桌面约 34–40px，移动端约 28–32px。
- 标题使用 `text-wrap: balance`，中短正文使用 `text-wrap: pretty`；根元素保持 macOS 字体抗锯齿。
- 状态数字或动态计数才使用等宽数字；静态版本号和装饰数字不强制使用。

### 4.3 色彩与表面

- 以现有墨绿、纸白和薄荷色为基础，加入 Noemion 的“语义频谱”：薄荷、青蓝、琥珀和朱橙只用于首屏图形、关键短语和分区细线。
- 卡片和入口行保持近方形或小圆角，不使用大面积胶囊卡片；圆形箭头是少量明确的行动符号。
- 深度表面优先使用透明多层阴影，内容分区仍使用细分隔线。图片或渲染图在浅色模式使用 `rgba(0, 0, 0, 0.1)` 内描边，深色模式使用 `rgba(255, 255, 255, 0.1)`。
- 图形资产必须服务于 NIR、NOBJ、符号、重定位、链接或可信装载等真实概念，禁止仅为装饰堆叠无意义科技纹理。

## 5. 动效契约

动效目标是形成快速、直接和可中断的手感，同时提高可访问性与性能。

| 场景 | 行为 | 时长与曲线 |
| --- | --- | --- |
| 首屏进入 | 标题、说明、行动区和右侧图形分组进入；透明度、4px 模糊和 12px 下移逐项归位 | 360–420ms，组间约 90ms，单次关键帧 |
| 顶部菜单 | 桌面悬停/聚焦改变底色；移动菜单和分组面板可反向中断 | 180–200ms，`cubic-bezier(.2,0,0,1)` |
| 内容行悬停 | 纸面轻微提亮，图形最多放大 1.025，箭头向右移动 4px | 150–200ms，仅过渡具体属性 |
| 三列卡片悬停 | 图片亮度或对比度轻微增加，标题和分隔线强调 | 150ms |
| 按压反馈 | 可点击按钮和入口缩放到 0.96，释放后平滑恢复 | 150ms，可中断 transition |
| 滚动进入 | 只对较大的叙事分区执行一次 12px/透明度进入；不做持续视差 | 360ms，分区间不连锁拖延 |
| 状态图标切换 | 两个图标同时保留并以透明度、0.25→1 缩放和 4px→0 模糊交叉切换 | 300ms，`cubic-bezier(.2,0,0,1)` |

禁止使用 `transition: all`、不可中断的交互关键帧、横向页面抖动、持续跟随鼠标的 3D 倾斜和无信息价值的循环动画。`will-change` 只在真实出现首帧卡顿时用于 transform、opacity 或 filter。`prefers-reduced-motion: reduce` 下取消进入、滚动和图形循环，仅保留即时状态变化。

首屏之后的同级章节标题不使用逗号或句末句号承担节奏，统一由标题两侧的双色章节横线建立停顿。横向阅读入口使用 5px 频谱描边，并通过首尾等价的角度渐变形成无跳帧持续循环；右侧行动箭头在明暗主题中保留可见静态圆环，悬停或键盘聚焦时以约 550ms 顺时针补画加粗圆环，减少动态效果时立即显示最终状态。

## 6. 响应式与可访问性

- 840px 及以上保留横向导航；839px 及以下变为品牌、状态和菜单按钮，按钮触控区域至少 44×44px。
- 首屏移动端先显示标题与状态，再显示对象图形；双栏论点变为标题在前、解释在后。
- 横向内容行在移动端变为“图形 / 标题与摘要 / 行动按钮”的纵向卡片，不依赖横向滚动。
- 主要断点以内容是否拥挤决定，建议在约 1100px、760px 和 480px 校验，而不是复制某个设备清单。
- 所有动效后的最终内容必须在无 JavaScript和减少动态效果设置下可读；图形使用有意义的替代文本或被标记为纯装饰。
- 焦点样式、键盘导航、标题层级、跳到正文链接和深浅色对比继续通过现有质量基线验证。

## 7. 维护边界

- `index.html` 只维护门户语义结构、内容顺序和真实入口。
- `_includes/site-header.html`、`_includes/site-footer.html` 与 `assets/directory.js` 维护全站共享导航，不在首页复制第二套数据。
- `assets/style.css` 中的门户规则以 `body[data-page-role="portal"]` 和明确组件类为作用域，不泄漏到规范、工具和手册页面。
- 图形保持自有、无外部运行依赖；不引入大型框架或动画依赖。
- 路由、状态或视觉契约变化时同步 README、设计规范、测试和内容审计，不保留旧入口或兼容样式。

## 8. 验收标准

1. 1440px 左右桌面视口下，页面呈现约 1200px 居中长画布，顶部导航、首屏双栏、论点双栏、入口行和三列网格形成一致的 Noemion 叙事节奏。
2. 390px 左右移动视口下，阅读顺序正确，首屏和全部内容块单列化，无横向溢出，菜单与行动按钮触控区域合格。
3. 首屏第一屏能直接回答 Noemion 解决什么问题、当前处于什么阶段，并提供至少一个明确阅读入口。
4. 所有图形和内容都与 Noemion 的对象工具链直接相关。
5. 交互反馈集中在 150–300ms，进入动效克制且支持减少动态效果；不存在 `transition: all`。
6. 首页仍明确区分已确认原则、设计提案、开放问题和未来能力，没有虚构版本、下载、背书、客户或实验结果。
7. `python3 tests/site_quality_test.py`、Jekyll 构建后的质量测试以及桌面/移动浏览器实测全部通过。

## 9. 重新评审条件

- 首页不再能在首屏回答项目问题、真实状态或下一阅读入口时，重新评审信息顺序。
- 语义频谱、对象面板或数据流动画无法对应真实对象概念时，删除或替换该视觉，不为保留品牌效果牺牲准确性。
- 新页面角色、导航组或发布状态出现时，同步评审首页入口、移动菜单、页脚和主题持久化行为。
